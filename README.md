# Love You More

A polished romantic one-page site inspired by the playful reveal flow of the reference page, now themed around a warm sunset-sky beach image.

Open `index.html` in a browser to view it. Edit the messages in `index.html` and the finale options in `script.js` to personalize it before sharing or deploying. The page uses optimized desktop and mobile hero images from `assets/`.
